/**
 * APIFiddle Sample Definition
 * Example workspace
 *
 * The version of the OpenAPI document: 0.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TestAttemptLuaChonInner from './TestAttemptLuaChonInner';

/**
 * The TestAttempt model module.
 * @module model/TestAttempt
 * @version 0.0.0
 */
class TestAttempt {
    /**
     * Constructs a new <code>TestAttempt</code>.
     * 
     * @alias module:model/TestAttempt
     * @param ngayBatDauThi {String} 
     * @param luaChon {Array.<module:model/TestAttemptLuaChonInner>} 
     */
    constructor(ngayBatDauThi, luaChon) { 
        
        TestAttempt.initialize(this, ngayBatDauThi, luaChon);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, ngayBatDauThi, luaChon) { 
        obj['ngayBatDauThi'] = ngayBatDauThi;
        obj['luaChon'] = luaChon;
    }

    /**
     * Constructs a <code>TestAttempt</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TestAttempt} obj Optional instance to populate.
     * @return {module:model/TestAttempt} The populated <code>TestAttempt</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TestAttempt();

            if (data.hasOwnProperty('thoiGianLamBai')) {
                obj['thoiGianLamBai'] = ApiClient.convertToType(data['thoiGianLamBai'], 'Number');
            }
            if (data.hasOwnProperty('ngayBatDauThi')) {
                obj['ngayBatDauThi'] = ApiClient.convertToType(data['ngayBatDauThi'], 'String');
            }
            if (data.hasOwnProperty('luaChon')) {
                obj['luaChon'] = ApiClient.convertToType(data['luaChon'], [TestAttemptLuaChonInner]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TestAttempt</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TestAttempt</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TestAttempt.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['ngayBatDauThi'] && !(typeof data['ngayBatDauThi'] === 'string' || data['ngayBatDauThi'] instanceof String)) {
            throw new Error("Expected the field `ngayBatDauThi` to be a primitive type in the JSON string but got " + data['ngayBatDauThi']);
        }
        if (data['luaChon']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['luaChon'])) {
                throw new Error("Expected the field `luaChon` to be an array in the JSON data but got " + data['luaChon']);
            }
            // validate the optional field `luaChon` (array)
            for (const item of data['luaChon']) {
                TestAttemptLuaChonInner.validateJSON(item);
            };
        }

        return true;
    }


}

TestAttempt.RequiredProperties = ["ngayBatDauThi", "luaChon"];

/**
 * @member {Number} thoiGianLamBai
 */
TestAttempt.prototype['thoiGianLamBai'] = undefined;

/**
 * @member {String} ngayBatDauThi
 */
TestAttempt.prototype['ngayBatDauThi'] = undefined;

/**
 * @member {Array.<module:model/TestAttemptLuaChonInner>} luaChon
 */
TestAttempt.prototype['luaChon'] = undefined;






export default TestAttempt;

